/**
 * [view-transition-name:unique-name] in both 2 deffirent components
 * [style.view-transition-name]='route-name' within routerLink directive !
 */

// lazy load animations
/**
 * @import { withViewTransitions } @from '@angular/router';
 *
 * @import provideAnimationsAsync() @from
 */
